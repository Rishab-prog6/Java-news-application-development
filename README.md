# Java-News-Application-development
This repo contains the source codes for the news paper app that has a fine GUI where user can view and search for news, uses the API of Tsinghua University GLM AI to generate an AI summary inside every news
